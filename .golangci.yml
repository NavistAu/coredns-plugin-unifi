version: "2"

# The v2 standard default set is errcheck, govet, ineffassign, staticcheck
# (which absorbed gosimple), and unused — the same list the v1 config
# enabled explicitly.
linters:
  exclusions:
    paths:
      - integration
